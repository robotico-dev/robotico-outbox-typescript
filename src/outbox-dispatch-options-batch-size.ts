import type { OutboxDispatchBatchSizeShape } from "./outbox-dispatch-batch-size-shape.js";

/** Maximum number of pending rows to load per outbox dispatch iteration. */
export type OutboxDispatchOptionsBatchSize = OutboxDispatchBatchSizeShape;
